# Configuration file for EventSetupTest_t

process TEST = {

        path p = { prod }

        es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "CSCDBGainsRcd"
                                     string tag = "CSCDBGains_new_popcon"
                                    } }
                                    bool loadAll = true
                                    #read from sqlite_file
				    string connect = "sqlite_file:DBGains.db"
                                    string timetype = "runnumber"
				    # read from database
				    #string connect="frontier://FrontierDev/CMS_COND_CSC"
	
			PSet DBParameters = {
			            untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
                                    untracked uint32 authenticationMethod = 1
 			}
                                   }
        untracked PSet maxEvents = {untracked int32 input = 1}
        source = EmptySource {}

        module prod = CSCGainsDBReadAnalyzer{}

        module output = AsciiOutputModule { }
        endpath ep = { output }
}
