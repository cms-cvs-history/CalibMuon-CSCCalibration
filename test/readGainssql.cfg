# Configuration file for EventSetupTest_t

process TEST = {

        path p = { prod }

        es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "CSCGainsRcd"
                                     string tag = "CSCGains_ideal"
                                    } }
                                    bool loadAll = true
                                    string connect = "sqlite_file:calib.db"
                                    string timetype = "runnumber"
                                    untracked string catalog = "file:PoolFileCatalog_calib.xml"

			PSet DBParameters = {
			            #untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
                                    #untracked uint32 authenticationMethod = 0
 			}
                                   }

        source = EmptySource {untracked int32 maxEvents = 1
                              untracked uint32 firstRun = 1
                              untracked uint32 numberEventsInRun = 1
                 }

        module prod = CSCGainsReadAnalyzer{}

        module print = AsciiOutputModule { }
        endpath ep = { print }
}
