# Configuration file for EventSetupTest_t

process TEST = {

        path p = { prod }

        es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "CSCDBCrosstalkRcd"
                                     string tag = "CSCDBCrosstalk_ideal"
                                    } }
                                    bool loadAll = true
			            #read from sqlite file
                                    #string connect = "sqlite_file:calibxtalk.db"
				    string timetype = "runnumber"
				    #untracked string catalog = "file:PoolFileCatalog_calibxtalk.xml"
				    #read from DB 
				    string connect = "frontier://FrontierDev/CMS_COND_CSC"
                                    
			PSet DBParameters = {
			            untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
                                    untracked uint32 authenticationMethod = 1
 			}
                                   }
        untracked PSet maxEvents = {untracked int32 input = 1}
        source = EmptySource {}

        module prod = CSCCrossTalkDBReadAnalyzer{}

        module print = AsciiOutputModule { }
        endpath ep = { print }
}
